name: Excavator

on:
  schedule:
    - cron: '0 */4 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  excavator:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Excavator
        uses: ScoopInstaller/Action-AutoPR@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
